# URL_Shortener

A Next.js URL shortener application with analytics, QR code generation, rate limiting, and Prisma-backed storage.

## Getting Started

1. Install dependencies:

```bash
npm install
```

2. Configure environment variables in `.env`.

3. Run the development server:

```bash
npm run dev
```

Open `http://localhost:3000` in your browser.
